package net.clahey.kinderdraw.shared.brand

import androidx.compose.ui.graphics.Color

// The Your Average Chris brand palette, and the only copy of it in Kotlin.
// Canonical definition, including each color's role, is `docs/brand.md` —
// change a value there first, then here.

val BrandLightBlue = Color(0xFF47AADC)
val BrandDarkNavy = Color(0xFF04325C)
val BrandLightNavy = Color(0xFF0757A1)

/** Highlight — the one branded detail meant to draw the eye, not a fill area. */
val BrandYellow = Color(0xFFFCD214)

/** For warm accents on light surfaces where [BrandYellow] washes out. */
val BrandDarkerYellow = Color(0xFFEBC413)

val BrandGreen = Color(0xFF148244)
