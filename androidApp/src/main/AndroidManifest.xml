<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      @spec CANVAS-UX-051

      No action bar over the drawing surface. See the User Experience LLD's
      Screen Composition.

      Light gives the window a white background, matching the canvas (see
      DefaultStyleSettings), so nothing flashes before Compose's first frame.
    -->
    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:theme="@android:style/Theme.DeviceDefault.Light.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
